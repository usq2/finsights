export interface Tokens {
  email: string;
  access_token: string;
  expiry_date: number;
  id_token: string;
  refresh_token: string;
  refresh_token_expires_in: number;
  scope: string;
  token: string;
}
